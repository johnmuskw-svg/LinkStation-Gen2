# —— LinkStation Modem API 基础配置 ——
API_TITLE=LinkStation Modem API
API_PREFIX=/v1

# 串口
SERIAL_PORT=/dev/ttyUSB2
BAUDRATE=115200

# 轮询与缓存
POLL_INTERVAL=1.0      # /v1/live 后台采集周期（秒）
INFO_TTL=30            # /v1/info 缓存（秒），后续一步我们会启用
LIVE_TTL=2             # 预留（若做接口级缓存）

# CORS 允许的来源（稍后一步启用中间件）
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 预留：管理口令（做控制指令时用）
AUTH_TOKEN=changeme
AUTH_REQUIRED=0

CTRL_ENABLE=1
CTRL_ALLOW_DANGEROUS=1
